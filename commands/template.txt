(Modules, or variables from other files, may be required here)

module.exports = {
    name: "<command name>", //self-explanatory
    aliases: [], //An array of strings; leave as is if no aliases
    description: "", //self-explanatory
    args: true/false, //Are args REQUIRED to use a command? Some commands may not have required args but will still take viable args (ex: help)
    usage: "", //How args should be used in a command. Leave as is if no args. Syntax based on Minecraft command syntax.
    guildOnly: true, //Determines if a command should only be usable 
    execute(message, args) {
        //Command-specific code goes here
    }
}